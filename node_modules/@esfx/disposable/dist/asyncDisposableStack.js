"use strict";
/*!
   Copyright 2019 Ron Buckton

   Licensed under the Apache License, Version 2.0 (the "License");
   you may not use this file except in compliance with the License.
   You may obtain a copy of the License at

       http://www.apache.org/licenses/LICENSE-2.0

   Unless required by applicable law or agreed to in writing, software
   distributed under the License is distributed on an "AS IS" BASIS,
   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   See the License for the specific language governing permissions and
   limitations under the License.
*/
Object.defineProperty(exports, "__esModule", { value: true });
exports.AsyncDisposableStack = void 0;
const asyncDisposable_1 = require("./asyncDisposable");
const asyncDisposable_2 = require("./internal/asyncDisposable");
const utils_1 = require("./internal/utils");
const weakAsyncDisposable = new WeakMap();
const weakDisposeAsync = new WeakMap();
const reportAsyncDisposableStackEnterDeprecation = (0, utils_1.createDeprecation)("Use 'AsyncDisposableStack.use()' instead.");
/**
 * Emulates Python's `AsyncExitStack`
 */
class AsyncDisposableStack {
    constructor() {
        const disposable = Object.create(asyncDisposable_1.AsyncDisposable.prototype);
        asyncDisposable_2.weakAsyncDisposableState.set(disposable, "pending");
        asyncDisposable_2.weakAsyncDisposableResourceStack.set(disposable, []);
        weakAsyncDisposable.set(this, disposable);
        weakDisposeAsync.set(this, null);
    }
    /**
     * Dispose this object's resources.
     *
     * NOTE: `disposeAsync` returns a bound method, so it can be extracted from `AsyncDisposableStack` and called independently:
     *
     * ```ts
     * const stack = new AsyncDisposableStack();
     * for (const f of files) stack.use(openFile(f));
     * const closeFiles = stack.disposeAsync;
     * ...
     * closeFiles();
     * ```
     */
    get disposeAsync() {
        let disposeAsync = weakDisposeAsync.get(this);
        if (disposeAsync === undefined)
            throw new TypeError("Wrong target");
        if (disposeAsync === null) {
            disposeAsync = __AsyncDisposableStack_prototype_disposeAsync__.bind(this);
            weakDisposeAsync.set(this, disposeAsync);
        }
        return disposeAsync;
    }
    use(value, onDispose = undefined) {
        const disposable = weakAsyncDisposable.get(this);
        if (!disposable)
            throw new TypeError("Wrong target");
        const disposableState = asyncDisposable_2.weakAsyncDisposableState.get(disposable);
        if (disposableState === "disposed")
            throw new ReferenceError("Object is disposed");
        if (disposableState !== "pending")
            throw new ReferenceError("Wrong target");
        if (value !== null && value !== undefined || onDispose) {
            const disposableResourceStack = asyncDisposable_2.weakAsyncDisposableResourceStack.get(disposable);
            (0, utils_1.AddDisposableResource)(disposableResourceStack, onDispose ? () => onDispose(value) : value, "async");
        }
        return value;
    }
    enter(value, onDispose = undefined) {
        reportAsyncDisposableStackEnterDeprecation();
        return this.use(value, onDispose);
    }
    /**
     * Moves all resources out of this `AsyncDisposableStack` and into a new `AsyncDisposableStack` and returns it.
     */
    move() {
        const disposable = weakAsyncDisposable.get(this);
        if (!disposable)
            throw new TypeError("Wrong target");
        const disposableState = asyncDisposable_2.weakAsyncDisposableState.get(disposable);
        if (disposableState === "disposed")
            throw new ReferenceError("Object is disposed");
        if (disposableState !== "pending")
            throw new ReferenceError("Wrong target");
        const disposableResourceStack = asyncDisposable_2.weakAsyncDisposableResourceStack.get(disposable);
        const newDisposable = Object.create(asyncDisposable_1.AsyncDisposable.prototype);
        asyncDisposable_2.weakAsyncDisposableState.set(newDisposable, "pending");
        asyncDisposable_2.weakAsyncDisposableResourceStack.set(newDisposable, disposableResourceStack);
        asyncDisposable_2.weakAsyncDisposableResourceStack.set(disposable, []);
        const newDisposableStack = Object.create(__AsyncDisposableStack_prototype__);
        weakAsyncDisposable.set(newDisposableStack, newDisposable);
        weakDisposeAsync.set(newDisposableStack, null);
        return newDisposableStack;
    }
    /**
     * Dispose this object's resources.
     */
    async [asyncDisposable_1.AsyncDisposable.asyncDispose]() {
        const disposable = weakAsyncDisposable.get(this);
        if (!disposable)
            throw new TypeError("Wrong target");
        const disposableState = asyncDisposable_2.weakAsyncDisposableState.get(disposable);
        if (disposableState === "disposed")
            return;
        if (disposableState !== "pending")
            throw new ReferenceError("Wrong target");
        asyncDisposable_2.weakAsyncDisposableState.set(disposable, "disposed");
        await (0, utils_1.DisposeResources)("async", asyncDisposable_2.weakAsyncDisposableResourceStack.get(disposable), /*suppress*/ false, /*completion*/ undefined);
    }
}
exports.AsyncDisposableStack = AsyncDisposableStack;
const __AsyncDisposableStack_prototype__ = AsyncDisposableStack.prototype;
const __AsyncDisposableStack_prototype_disposeAsync__ = AsyncDisposableStack.prototype[asyncDisposable_1.AsyncDisposable.asyncDispose];
Object.defineProperty(__AsyncDisposableStack_prototype__, Symbol.toStringTag, { configurable: true, value: "AsyncDisposableStack" });
//# sourceMappingURL=asyncDisposableStack.js.map