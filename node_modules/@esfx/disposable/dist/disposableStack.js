"use strict";
/*!
   Copyright 2019 Ron Buckton

   Licensed under the Apache License, Version 2.0 (the "License");
   you may not use this file except in compliance with the License.
   You may obtain a copy of the License at

       http://www.apache.org/licenses/LICENSE-2.0

   Unless required by applicable law or agreed to in writing, software
   distributed under the License is distributed on an "AS IS" BASIS,
   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   See the License for the specific language governing permissions and
   limitations under the License.
*/
Object.defineProperty(exports, "__esModule", { value: true });
exports.DisposableStack = void 0;
const disposable_1 = require("./disposable");
const disposable_2 = require("./internal/disposable");
const utils_1 = require("./internal/utils");
const weakDisposable = new WeakMap();
const weakDispose = new WeakMap();
const reportDisposableStackEnterDeprecation = (0, utils_1.createDeprecation)("Use 'DisposableStack.use()' instead.");
/**
 * Emulates Python's `ExitStack`
 */
class DisposableStack {
    /**
     * Creates a new DisposableStack.
     */
    constructor() {
        const disposable = Object.create(disposable_1.__Disposable_prototype__);
        disposable_2.weakDisposableState.set(disposable, "pending");
        disposable_2.weakDisposableResourceStack.set(disposable, []);
        weakDisposable.set(this, disposable);
        weakDispose.set(this, null);
    }
    /**
     * Dispose this object's resources.
     *
     * NOTE: `dispose` returns a bound method, so it can be extracted from `DisposableStack` and called independently:
     *
     * ```ts
     * const stack = new DisposableStack();
     * for (const f of files) stack.use(openFile(f));
     * const closeFiles = stack.disposeAsync;
     * ...
     * closeFiles();
     * ```
     */
    get dispose() {
        let dispose = weakDispose.get(this);
        if (dispose === undefined)
            throw new TypeError("Wrong target");
        if (dispose === null) {
            dispose = __DisposableStack_prototype_dispose__.bind(this);
            weakDispose.set(this, dispose);
        }
        return dispose;
    }
    use(value, onDispose = undefined) {
        const disposable = weakDisposable.get(this);
        if (!disposable)
            throw new TypeError("Wrong target");
        const disposableState = disposable_2.weakDisposableState.get(disposable);
        if (disposableState === "disposed")
            throw new ReferenceError("Object is disposed");
        if (disposableState !== "pending")
            throw new TypeError("Wrong target");
        if (value !== null && value !== undefined || onDispose) {
            const disposableResourceStack = disposable_2.weakDisposableResourceStack.get(disposable);
            (0, utils_1.AddDisposableResource)(disposableResourceStack, onDispose ? () => onDispose(value) : value, "sync");
        }
        return value;
    }
    enter(value, onDispose = undefined) {
        reportDisposableStackEnterDeprecation();
        return this.use(value, onDispose);
    }
    /**
     * Moves all resources out of this `DisposableStack` and into a new `DisposableStack` and returns it.
     */
    move() {
        const disposable = weakDisposable.get(this);
        if (!disposable)
            throw new TypeError("Wrong target");
        const disposableState = disposable_2.weakDisposableState.get(disposable);
        if (disposableState === "disposed")
            throw new ReferenceError("Object is disposed");
        if (disposableState !== "pending")
            throw new TypeError("Wrong target");
        const disposableResourceStack = disposable_2.weakDisposableResourceStack.get(disposable);
        const newDisposable = Object.create(disposable_1.__Disposable_prototype__);
        disposable_2.weakDisposableState.set(newDisposable, "pending");
        disposable_2.weakDisposableResourceStack.set(newDisposable, disposableResourceStack);
        disposable_2.weakDisposableResourceStack.set(disposable, []);
        const newDisposableStack = Object.create(__DisposableStack_prototype__);
        weakDisposable.set(newDisposableStack, newDisposable);
        weakDispose.set(newDisposableStack, null);
        return newDisposableStack;
    }
    /**
     * Dispose this object's resources.
     */
    [disposable_1.Disposable.dispose]() {
        const disposable = weakDisposable.get(this);
        if (!disposable)
            throw new TypeError("Wrong target");
        const disposableState = disposable_2.weakDisposableState.get(disposable);
        if (disposableState === "disposed")
            return;
        if (disposableState !== "pending")
            throw new TypeError("Wrong target");
        disposable_2.weakDisposableState.set(disposable, "disposed");
        (0, utils_1.DisposeResources)("sync", disposable_2.weakDisposableResourceStack.get(disposable), /*suppress*/ false, /*completion*/ undefined);
    }
}
exports.DisposableStack = DisposableStack;
const __DisposableStack_prototype__ = DisposableStack.prototype;
const __DisposableStack_prototype_dispose__ = DisposableStack.prototype[disposable_1.Disposable.dispose];
Object.defineProperty(__DisposableStack_prototype__, Symbol.toStringTag, { configurable: true, value: "DisposableStack" });
//# sourceMappingURL=disposableStack.js.map